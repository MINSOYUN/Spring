<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.momo.mapper.MemberMapper">

	<select id="login" resultType="com.momo.vo.Member">
		select id, pw, name, adminyn, status, grade from member where id=#{id} and pw=#{pw}
	</select>
	
	<insert id="insert">
		insert into member(id, pw, name, adminyn, status, grade) values(#{id}, #{pw}, #{name}, #{adminyn}, 'N', #{grade})
	</insert>
	
	<!-- <delete id="delete">
		delete from member where name=#{name} 
	</delete>  -->

</mapper>